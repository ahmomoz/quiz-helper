# Tasks: Fix CI Build

1.  [x] **Rename Directory**
    -   Use `git mv` to rename `src/components/Theme` to `src/components/theme` (via temp directory).
2.  [ ] **Validation**
    -   Verify `git ls-files` shows the correct lowercase path.
